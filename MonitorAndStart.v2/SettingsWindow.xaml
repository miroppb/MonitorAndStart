<Window x:Class="MonitorAndStart.v2.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonitorAndStart.v2"
        mc:Ignorable="d"
        Background="LightPink"
        Title="SettingsWindow" Height="450" Width="529" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Notification Mechanism:" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" />
        <ComboBox Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding NotificationEngines}" SelectedItem="{Binding SelectedNotificationEngine}" Margin="10,40,80,40" />

        <TextBlock Text="API/Channel" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" />
        <TextBox Text="{Binding APIChannel}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,40,80,40" />

        <Button Content="Save" Command="{Binding SaveCommand}" Grid.Row="3" Grid.Column="1" Margin="40" />
    </Grid>
</Window>
