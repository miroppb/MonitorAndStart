<Window x:Class="MonitorAndStart.v2.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonitorAndStart.v2"
        mc:Ignorable="d"
        Background="LightPink"
        Title="SettingsWindow" Height="450" Width="529" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Notification Mechanism:" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" />
        <ComboBox Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding NotificationEngines}" SelectedItem="{Binding SelectedNotificationEngine}"
                  HorizontalAlignment="Left" VerticalAlignment="Center" Width="263" Margin="10,0,0,0" />

        <TextBlock Text="API/Channel" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" />
        <TextBox Text="{Binding APIChannel}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                 HorizontalAlignment="Left" VerticalAlignment="Center" Width="263" Margin="10,0,0,0"/>

        <TextBlock Text="Console Engine" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" />
        <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding ConsoleEngines}" SelectedItem="{Binding SelectedConsoleEngine}"
                  HorizontalAlignment="Left" VerticalAlignment="Center" Width="263" Margin="10,0,0,0"/>

        <TextBlock Text="Run Server:" Grid.Row="3" HorizontalAlignment="Right" VerticalAlignment="Center" />
        <CheckBox IsChecked="{Binding RunServer}" Grid.Row="3" Grid.Column="1" Margin="10,26,0,27" />
        <TextBlock Text="Server initialization happens on application startup" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="10" />

        <TextBlock Text="Port:" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="14" />
        <TextBox Text="{Binding ServerPort}" Grid.Row="3" Grid.Column="2"
                    HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" Width="50"/>

        <Button Content="Save" Command="{Binding SaveCommand}" Grid.Row="5" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="15, 2" />
    </Grid>
</Window>
